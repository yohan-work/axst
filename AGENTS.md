# AGENTS.md — 이 레포에서 작업할 때의 규약

사람 기여자와 코딩 에이전트 모두에게 적용된다. 기여 절차 전반은 [CONTRIBUTING.md](CONTRIBUTING.md), 남은 과제는 [docs/12-open-issues.md](docs/12-open-issues.md).

## 원칙

1. **마크다운이 source of truth다.** 별도 DB·스프레드시트를 만들지 않는다. 문항 통계(`stats`)도 문항 파일 frontmatter 안에 둔다.
2. **모든 응시자용 콘텐츠는 한국어로 쓴다.** 메타 논의(설계 근거)도 한국어로 쓴다.
3. **`forms/`의 모든 값은 `items/`에서 나온다.** 다만 유지 방식이 둘로 갈린다.

   | 파일 | 유지 | 드리프트 방지 |
   |---|---|---|
   | `form-A.web.html` | **생성물** ← `build_web_form.py` | 재생성 |
   | `form-A.md` · `form-A.answers.md` · `MANIFEST.yml` | **손으로 유지** | **린트가 파생값을 전수 대조** |

   지필 응시본에는 기계가 만들 수 없는 편집 판단이 들어 있다 — 주관식을 지필용으로 압축한 문장, 블록 헤더, 안내문. 그래서 생성하지 않고 **검증한다.** 정답·축·난이도·최악·배점·stance·시간, 문항 본문, 공통 지문, 축별 집계표를 전부 문항 파일과 대조한다.

   > **정답지의 정답이 문항 파일과 어긋나면 그 문항은 전원 오채점된다.** 이 검사가 그것만은 반드시 막는다.
4. **`pilot/responses/`는 커밋하지 않는다.** 사내 진단 응답은 개인정보이고, 레포에 들어가면 무기명 보장이 거짓이 된다.

## 문항을 추가·수정할 때

**순서를 지킨다.** [docs/01-axes.md](docs/01-axes.md)의 축 결정트리 → [docs/03-item-writing-rules.md](docs/03-item-writing-rules.md)의 출제 규칙 → 작성 → [docs/02-blueprint.md](docs/02-blueprint.md)의 배분표 갱신 → 린트.

**커밋 전에 반드시 실행한다.**

```
python3 scripts/build_web_form.py   # 문항을 고쳤으면 온라인 응시본 재생성
python3 scripts/lint_items.py
python3 -m unittest discover -s tests   # 채점기를 고쳤으면
```

CI(`.github/workflows/ci.yml`)가 같은 검사와 응시본 재생성 일치 여부를 확인한다.

채점은 `python3 scripts/score.py <응답.json>`. 정답을 `items/`에서 읽으므로 `forms/`와 무관하게 동작한다.

축 배분·난이도·정답 위치·stance 균형·시간 예산·선택지 길이 비율·`distractor_dx` 완전성·금지 패턴·공통 지문 길이·`stem_ref` 무결성·응시본 정답 누출·응시본↔문항 동기화(주관식 ①~④ 물음 포함)·페르소나 `anchor_items`·상대 링크를 검사한다. `forms/`를 손으로 고치면 동기화 검사에서 걸린다.

온라인 응시본도 같은 검사를 받는다 — 정답 누출, 선택지 일치, 그리고 **문항 고유 자료 누락**(블록 문항의 `(STEM-00N 참조)` 뒤에 붙는 검증 대상 AI 출력문 등). 이걸 빠뜨리면 답할 수 없는 문항이 된다.

### 축 배정 결정트리 (요약 — 정본은 docs/01-axes.md)

순서대로 실행하고 **먼저 걸리는 곳이 축**이다.

1. 오답이 규정·권리·계약 **위반**을 낳는가? (정확성이 아니라 허용성) → **RSK**
2. 이미 생성된 산출물을 제시하고 그 결함·신뢰도를 묻는가? → **VER**
3. AI에게 줄 지시문·조건·형식을 묻는가? → **DSN**
4. 과업이 2단계 이상 또는 2인 이상이고, 정답이 순서·핸드오프·전파를 바꾸는가? → **FLW**
5. 단일 과업의 위임 범위·최종 판단 보유자를 묻는가? → **DEL**

결정트리 몇 번에서 걸렸는지를 `axis_note`에 반드시 1줄로 남긴다.

### 출제 규칙 (요약 — 정본은 docs/03-item-writing-rules.md)

**절대 금지**
- 툴 상식 문항 ("다음 중 Claude 모델이 아닌 것은", "프롬프트 엔지니어링의 정의로 옳은 것은")
- "위 모든 것", "정답 없음", "A와 B", 절대어(항상/절대/모든/반드시)가 오답에만 등장하는 배치
- 정답에만 붙는 완충어(우선/먼저/일단)
- 1문항이 2개 축에 걸치는 것 — 그런 아이디어는 MC로 쓰지 않고 FR로 보낸다

**필수**
- 1문항 = 1축. `axis_note` 기재
- 4개 선택지는 **단 하나의 결정 차원**에서만 다르다
- 최장 선택지 ≤ 최단 선택지 × 1.25
- 모든 선택지에 `distractor_dx` 기재. 오개념을 이름 붙일 수 없는 오답은 필러이므로 폐기
- 허용 유형 3종만: `SJT` / `산출물비평` / `최선차선`

### 글자수·시간 상한

| 항목 | 상한 |
|---|---|
| 공통 지문 | 200자 |
| 단독 문항 지문 | 120자 |
| 선택지 4개 합계 | 240자(단독) / 200자(블록 내) |
| `time_sec` | 단독 70초 / 블록 내 40초 / 최선–최악 +10초 |
| 폼 전체 `time_sec` 합계 | 1200초 (20분) |

## 파일 구조

문항 파일은 **YAML frontmatter + 고정 H2 섹션**이다. 한국어 산문을 YAML에 넣지 않는다(따옴표·이스케이프 지옥). 섹션 순서를 바꾸지 않는다 — 파싱이 순서에 의존한다.

```
---
(frontmatter)
---

## 상황
## 질문
## 선택지
## 해설
## 오답 진단
```

스키마 정본은 [docs/02-blueprint.md](docs/02-blueprint.md)의 "frontmatter 스키마" 절. `items/mc/_template.md`를 복사해서 시작한다.

## 검증 규율

이 레포에서 실제로 사고가 났던 것들이다.

1. **검사를 추가하면 그 자리에서 고의로 깨뜨려 FAIL을 확인하고 원복한다.** 정답지의 정답을 조작해도 린트가 PASS하던 구멍이 있었고, 정규식 버그로 단독 문항 8개를 놓친 적이 있다.
2. **파싱할 섹션을 못 찾으면 폴백하지 말고 실패시킨다.** 섹션을 못 찾은 측정 스크립트가 파일 전체로 폴백해 글자수가 2배로 부풀었고, 존재하지 않는 결함 2건을 보고했다가 철회했다.
3. **에이전트(서브에이전트 포함)의 자기 보고를 근거로 쓰지 않는다.** 원문을 읽는 패스만이 근거다.
4. **LLM 채점은 차원별 독립 컨텍스트로 돌린다.** 캘리브레이션 회차에는 판단형 패스에 앵커를 주지 않는다. [rubrics/llm-scorer-prompt.md](rubrics/llm-scorer-prompt.md).
5. **앵커를 추가할 때 답안을 쓴 쪽이 점수까지 매기지 않는다.** 점수를 비운 채 다른 모델에 먼저 채점시키고, 의도와 수렴할 때만 확정한다.
6. **루브릭·채점 규칙 변경은 임의로 반영하지 않는다.** [docs/12-open-issues.md](docs/12-open-issues.md) A절의 항목은 설계 결정이 먼저다.

## 채점·리포트를 수정할 때

**축별 점수를 숫자로 내보내지 않는다.** 축당 4문항으로는 2/4와 3/4가 통계적으로 구별되지 않는다. 축 결과는 3단 플래그(강점 신호 / 판정 보류 / 보완 필요)로만 보고하고, 개인 리포트에 축별 레이더 차트를 그리지 않는다. 근거와 금지 문구 목록은 [docs/04-scoring.md](docs/04-scoring.md).

## 커밋

문항을 추가·수정하면 `docs/99-changelog.md`에 한 줄을 남긴다. 문항 id와 무엇을 왜 바꿨는지.
