# 99. 변경 기록

문항을 추가·수정하면 한 줄을 남긴다. 문항 id와 **무엇을 왜 바꿨는지**.

## 2026-09-16 — 초기 구축

레포 신설. 설계 근거는 [AI 교육을 받고도 82%는 현업에서 쓰지 않습니다](https://blog.bloomworld.ai/why-ai-training-doesnt-stick/) (Bloom, 2026-09-15).

### 설계 결정

| 결정 | 근거 |
|---|---|
| 평가 축을 5축으로 재설계 (팀스파르타 4축 미채용) | 원문의 결론 "무엇을 AI에게 맡길 것인가"가 기존 4축에 없다. 위임 판단력을 1축으로 신설, 데이터 분석은 검증 축에 흡수 |
| 축 배분 4/4/4/4/4 (초안 5/4/4/4/3에서 변경) | 3문항 축은 플래그조차 낼 수 없다. 위임 판단력의 비중은 FR-01에서 확보 |
| 축별 결과를 점수 대신 3단 플래그로 | 축당 4문항에서 2/4와 3/4는 통계적으로 구별되지 않는다 |
| 주관식 '리스크 처리' 차원 삭제 → 감점 규칙으로 이동 | 분산이 없고, 한 줄로 페이킹되며, 채점자 간 합의가 불가능하다. 그 자리에 '업무 전환 효과' 차원 신설 |
| 공통 지문 4블록 도입 + 35분 | 단독 20문항이면 객관식만 27분이 되어 주관식이 들어갈 자리가 없다. 지문을 얕게 깎으면 SJT의 전제가 무너진다 |
| 24문항 작성 → 20문항 배포 | 파일럿 탈락 문항을 예비로 교체. 예비는 판단 난이도가 높은 축(DEL·VER·FLW·RSK)에 배치 |
| 2트랙 정책(진단/인증) 명문화 | 현대위아의 HDAT 자격 확대 사례 등 인사 연동 압력이 실재한다. 겸용하면 진단 데이터가 오염된다 |
| 개인 리포트 레이더 차트 금지 | intoAX가 아티클당 5문항 퀴즈로 러닝 레이더를 그리는 것이 과잉 주장의 실례 |
| 주관식 프롬프트 첨부 요구 | Telta의 내장 LLM 대화 로그 채점에 대한 지필 대용 |
| 처방을 진단 전 스폰서 합의문으로 | 진단 결과를 교육 설계에 반영한 조직이 3.6%. 문항 품질로는 해결되지 않는다 |

### 작성한 문항

| 축 | 배포 | 예비 |
|---|---|---|
| DEL | `MC-DEL-001` ~ `004` | `MC-DEL-005` |
| DSN | `MC-DSN-001` ~ `004` | — |
| VER | `MC-VER-001` ~ `004` | `MC-VER-005` |
| FLW | `MC-FLW-001` ~ `004` | `MC-FLW-005` |
| RSK | `MC-RSK-001` ~ `004` | `MC-RSK-005` |
| 주관식 | `FR-01` `FR-02` | — |

공통 지문 `STEM-001` ~ `004`, 페르소나 6종, 자기보고 3문항, 앵커 답안 12개.

전 문항 `status: 초안`. 린트 전 항목 PASS ([forms/MANIFEST.yml](../forms/MANIFEST.yml)).

### 초기 린트에서 수정한 것

`scripts/lint_items.py`를 작성해 실행한 결과 선택지 길이 비율 위반 5건이 검출됐다 — `MC-DEL-003` `MC-DSN-004` `MC-VER-003` `MC-VER-004` `MC-VER-005`. **그중 4건은 정답이 가장 긴 선택지**였으므로 응시자가 내용을 모른 채 길이로 정답을 찍을 수 있는 상태였다. 의미를 바꾸지 않고 비율을 1.14 이하로 맞췄고, 응시본도 함께 갱신했다. 육안으로 잡히지 않는 유형이므로 린트 자동화의 가치가 가장 큰 항목이다.

린트에 응시본↔문항 동기화 검사를 넣었다. `forms/`는 생성물이므로 손으로 고치면 드리프트가 생기는데, 그것을 기계가 잡는다.

### 다음 할 일

1. [2단 자가 응시와 채점 리허설](07-validity-plan.md)
2. 파일럿 10~15명
3. `scripts/lint_items` 자동화

---

## 2026-09-16 — 2단 채점 리허설 (회차 C0)

문항 수정 없음. 검증 실행과 결함 기록만 추가했다.

### 실행한 것

[07-validity-plan.md](07-validity-plan.md) 2단의 채점 리허설. 앵커 12개(FR-01 6 + FR-02 6)와 페이킹 시험 답안 2개를 [llm-scorer-prompt.md](../rubrics/llm-scorer-prompt.md) 프로토콜(차원별 독립 4패스 + 감점 1패스)로 채점했다.

독립성 확보를 위해 패스마다 별도 컨텍스트를 쓰고, 배치 순서를 섞고, 레벨 라벨·부여 점수·출제자 볼드 강조를 제거하고, 루브릭 외 파일 접근을 차단했다. **그래도 앵커를 쓴 것과 같은 모델 계열이 채점했으므로 독립 검증이 아니다** — 일치는 타당도의 증거가 아니고 불일치만 신호다.

### 결과

| 통과 조건 | 판정 |
|---|---|
| D1 차이 전부 ±1 이내 | 통과 (12개 중 10개 일치, 2개 −1) |
| FR-02 L1-b 감점 −5 탐지 | 통과 |
| L1-b 이중 처벌 없음 | 통과 |
| FR-01 페이킹 답안 D2·D4 상한 2점 | 통과 (D2 4→2, D4 5→2) |
| FR-02 페이킹 답안 D2·D3 상한 3점 | 부분 통과 (D3만 실질 작동) |

**통과 조건은 전부 충족했으나 결함 10건이 나왔다.** 통과 조건이 D1만 검사해서 D2·D3의 앵커 오류 5건이 통과 판정에 가려졌다.

### 추가한 것

- `docs/10-calibration-findings.md` 신설 — 결함 10건과 처리 계획
- `pilot/item-analysis.md` — 회차 C0 캘리브레이션 이력, 전 차원 대조표, 차원별 수렴도, 페이킹 방어 확인 결과
- `docs/07-validity-plan.md` — 2단을 "채점 리허설 완료 / 시간 측정 대기"로 갱신

### 수정하지 않은 것 (의도적)

결함 10건을 전부 미수정으로 남겼다. 둘은 설계 판단이 필요하다.

- **갈래 A** — FIN 페르소나에서 감점 −5가 과잉 발동한다. 앵커 부여값(0)과 규칙(−5)이 모순이고, FIN 선택자 3명 중 2명이 감점을 받았다. 감점 발생률이 조직 리포트의 RSK 보조 지표인데 **페르소나 선택에 오염된다**. 앵커를 고칠지 규칙 범위를 좁힐지 결정이 필요하다
- **갈래 B** — 페이킹 방어가 D2·D4에만 있어서, 업무 내용이 한 글자도 없는 답안이 FR-01에서 14/20을 받는다. D1·D3에도 상한을 걸지 결정이 필요하다

나머지 8건(앵커 부여값 정정 5건, 프로토콜·문서 문구 3건)은 갈래 결정 후 일괄 반영한다.

### 추가 — 시간 예산 정합성 검사

소요 시간 실측은 사람 몫이므로, 대신 각 문항의 `time_sec` 배정이 실제 글자 부피와 맞는지 기계적으로 검사했다.

**1차 검사는 틀렸고 철회했다.** 측정 스크립트가 공통 지문의 섹션 헤더(`## 지문`) 대신 객관식용(`## 상황`)을 찾았고, 매치에 실패하자 파일 전체(`## 설계 메모` 포함)로 폴백해 글자수가 약 2배로 부풀었다. 그 결과 지문이 상한을 최대 +53% 초과한다고 보고했으나 **실제는 124~152자로 전부 200자 안**이다. 지문 읽기 속도도 526자/분이 아니라 213자/분이다.

정정된 전체 추정은 **32~35분**으로 목표(35분 ±3분) 안이다.

유효하게 남은 것은 둘이다.

- 상한 초과는 없었으나 **린트가 공통 지문 길이를 검사하지 않았다.** 상한 200자는 그대로 두고 검사만 추가했다 — 공통 지문 길이(실패), `chars` frontmatter 정합(경고), `stem_ref` 참조 무결성(실패)
- 주관식 13분에서 읽기 1,410자와 쓰기 800자를 빼면 두 문항 합쳐 구상에 쓸 시간이 3분이다. 상한 초과가 아니라 여유 부족이므로 파일럿에서 미완성률을 관찰한다

**교훈은 검사 코드 쪽이다.** 섹션을 못 찾으면 폴백하지 말고 실패해야 한다. 같은 버그가 다른 검사에서 조용히 통과할 수 있다.

---

## 2026-09-16 — 회차 C0 결함 반영

결함 11건 전부 반영. 설계 판단이 필요한 셋을 먼저 결정했다.

| 갈래 | 결정 | 근거 |
|---|---|---|
| **A** FIN 페르소나 감점 과잉 | **앵커를 고친다** | 규칙 텍스트가 맞고 앵커가 틀렸다. 공시 전 재무데이터를 그대로 투입하겠다는 것은 실제 위반이다. 대가로 FIN 선택자의 감점률이 구조적으로 높아지므로, **페르소나별 감점률을 따로 집계**하고 조직 리포트에 페르소나 구성을 함께 보고한다 |
| **B** 페이킹 방어 절반 작동 | **네 차원 전부 상한** | 위임 경계와 검증 설계는 고유명사 없이 형식만으로 쓸 수 있다. 상한이 D2·D4에만 있으면 내용 없는 답안이 14/20을 받는다. 네 차원에 걸면 8/20에 갇힌다 |
| **C** 공통 지문 상한 | **상한 유지, 검사만 추가** | 1차 측정이 틀렸다(아래). 초과가 없었으므로 상한을 올릴 이유가 없다. 다만 상한이 기계에 없던 것은 사실이라 린트에 검사를 추가했다 |

### 앵커 부여값 정정

독립 채점에서 어긋난 지점을 루브릭에 맞춰 고쳤다. 정정 사유를 앵커 파일 각 항목에 인용과 함께 남겼다.

| | L1-a | L1-b | L3-a | L3-b | L5-a | L5-b |
|---|---|---|---|---|---|---|
| **FR-01** 합계 | 4 → **5** | 7 | 14 | 11 | 20 | 20 → **19** |
| **FR-02** 합계 | 6 → **1** | 2 | 14 → **13** | 15 → **13** | 20 | 20 → **19** |

가장 뼈아픈 것은 **"20/20 만점" 앵커 2개가 만점이 아니었다**는 것이다. 두 L5-b 모두 D2 요소 ⑤(톤/품질 기준·예시)가 없어 최대 4점이다. 만점 앵커가 만점이 아니면 캘리브레이션의 상한 기준점이 무너진다.

`L3-b` 둘은 D1을 3 → 2로 내렸다. 겉보기 human-in-the-loop 규칙의 "최대 3점"을 **상한이 아니라 부여 점수로 오해**한 것이 원인이다. 그 결과 이제 **상한이 실제로 결합하는 앵커가 없다** — 파일럿 응답에서 보강해야 한다.

### 프로토콜 수정

- **통과 조건을 네 차원 전부 ±1로 확대.** D1만 검사하던 조건이 D2·D3의 앵커 오류 5건을 통과시켰다
- **캘리브레이션 회차 예외 신설.** 판단형 템플릿은 앵커를 프롬프트에 넣게 되어 있는데, 1단계에서는 채점 대상이 앵커 자신이라 순환이 된다. 그 회차에는 앵커를 주지 않고 루브릭 레벨 요건만으로 채점한다
- **IRR 위험도 표를 실측으로 교체.** 설계 당시 D1을 유일한 저신뢰 차원으로 봤으나 실측에서 가장 수렴이 안 된 차원은 **D2**였다(12개 중 6개, 전부 같은 방향). 카운트형이라고 신뢰도가 높은 것이 아니라 요소의 인정 기준이 명확한지가 결정한다

### 린트 추가

공통 지문 길이(≤200자, 실패) · `chars` frontmatter 정합(±10자, 경고) · `stem_ref` 참조 무결성(실패).

### 반영하지 않은 것

**회차 C1 재검증을 아직 하지 않았다.** 정정된 앵커로 캘리브레이션을 다시 돌려야 하고, 같은 모델 계열이면 여전히 독립 검증이 아니므로 다른 계열 모델이나 사람을 붙이는 편이 낫다.

---

## 2026-09-16 — 회차 C1 재검증 (교차 모델)

문항 수정 없음. 정정된 앵커로 캘리브레이션을 다시 돌렸다.

**채점 모델을 바꿨다.** 앵커를 쓴 것은 Opus, 채점은 Sonnet이다. C0의 가장 큰 한계 — 같은 모델이 자기가 쓴 답을 맞히는 것 — 를 줄이기 위해서다. 배치도 새로 섞었다.

### 결과 — 전부 통과

| 조건 | 결과 |
|---|---|
| 네 차원 전부 ±1 이내 | 통과. 60개 차원 점수 중 46개 완전 일치, **±2 이상 0개** |
| 감점 −5 탐지 | 통과 (FR-02 L1-a·L1-b) |
| FR-01 페이킹 답안 상한 | 통과. 합계 **8/20** (C0에서는 14/20) |
| FR-02 페이킹 답안 상한 | 통과 |

**C0의 ±2 불일치 2건이 정정 후 전부 해소됐다.** 갈래 A(감점 −5)·갈래 B(네 차원 상한) 결정과 앵커 정정 5건이 **다른 모델에서 같은 값·같은 근거로 재현**됐다.

페이킹 방어의 대조쌍이 깨끗하다. `L5-a`(실제 만점 앵커)와 페이킹 답안은 골격이 거의 같고 고유명사 유무만 다른데 **20/20 대 8/20**으로 갈린다.

### 새 결함 3건

- **14 [중간] D2 요소 ①(수신자·용도)의 인정 기준이 모호하다.** C1의 ±1 불일치 4건이 전부 ① 판정이고 방향이 같다. C0에서도 같은 자리였다. 서로 다른 모델이 두 회차에 걸쳐 같은 방향으로 어긋나는 것은 루브릭 문구 문제다. FR-01은 ①이 지시문 안에 있어야 하는지를 정하지 않았고, 위치를 못박은 FR-02 쪽이 더 일관됐다
- **15 [중간] D4 2점 칸이 수치를 요구하는지 불명확하다.** FR-01 L1-a가 C0 2점 / C1 1점으로 갈렸다. 예시("주 3시간 단축")에는 수치가 있는데 규칙 문장에는 요구가 없다. 공통 원칙("형용사가 아니라 수치·고유명사·예시")을 따르면 1점이 맞고, 그러면 결함 10의 정정을 되돌려야 한다
- **16 [높음] FIN 페르소나 감점 편향이 재확인됐고 더 나빠졌다.** 두 회차·두 모델에서 FIN 답안은 예외 없이 감점을 받았다(C0 2/2, C1 2/2). 페르소나 조건을 하나도 쓰지 않은 범용 답안까지 −5가 나왔다 — **감점이 응답자가 제안한 행위가 아니라 페르소나가 가진 조건에 의해 발동한다.** 같은 답안을 MFG나 STAFF로 내면 0점이다. 같은 답안에 C0은 −2, C1은 −5로 모델 간 3점 차이도 났다

갈래 A 결정 자체는 옳았으나(규칙이 맞고 앵커가 틀렸다), 그 결정이 드러낸 구조적 편향은 별개 문제로 남았다.

### 여전히 남는 한계

교차 벤더 검증이 아니다. Opus와 Sonnet은 다른 모델이지만 같은 계열이고, 같은 편향을 공유하면 일치해도 타당도의 증거가 되지 않는다. 인간 채점자는 아직 한 번도 참여하지 않았다.
