# 10. 캘리브레이션 결함 기록 — 회차 C0

[07-validity-plan.md](07-validity-plan.md) 2단(자가 응시와 채점 리허설)의 채점 리허설에서 발견된 결함이다.
집계표는 [pilot/item-analysis.md](../pilot/item-analysis.md) "회차 C0".

**갈래 A·B·C의 결정이 내려졌고 전 항목을 반영했다.** 반영 내역은 맨 아래 [처리 결과](#처리-결과). 재검증(회차 C1)은 아직 하지 않았다.

---

## 실행 조건

| 항목 | 값 |
|---|---|
| 일자 | 2026-09-16 |
| 대상 | 앵커 12개(FR-01 6 + FR-02 6) + 페이킹 시험 답안 2개 |
| 프로토콜 | [rubrics/llm-scorer-prompt.md](../rubrics/llm-scorer-prompt.md) — 차원별 독립 4패스 + 감점 1패스 |
| 독립성 확보 | 패스마다 별도 컨텍스트. 배치 순서 섞음, 레벨 라벨·부여 점수·출제자 볼드 강조 제거, 루브릭 외 파일 접근 차단 |

### 이 결과의 한계 (해석 전에 읽는다)

1. **독립 검증이 아니다.** 앵커와 부여 점수를 쓴 것과 같은 모델 계열이 채점했다. **일치 항목은 타당도의 증거가 아니다** — 같은 편향을 공유할 뿐이다. 반대로 **불일치 항목은 신호가 강하다**: 정답지 없이 다시 읽었을 때 어긋났다는 것은 앵커나 루브릭 자체가 모호하다는 뜻이다. 아래 결함은 전부 불일치에서 나왔다.
2. **temperature 0을 설정하지 못했다.** 결정론적 재현이 보장되지 않는다.
3. **D1·D4 패스에 앵커를 주지 않았다** (결함 7의 순환을 피하는 유일한 방법). 실제 파일럿 채점은 앵커를 받으므로 조건이 다르다.
4. **앵커의 볼드 강조를 제거했다.** 볼드는 출제자 주석이지 응답자 원문이 아니고 실제 응답에는 없다. 이것이 D2 불일치 일부를 만들었을 수 있는데, 그렇다면 **원래 부여 점수가 볼드 단서에 의존하고 있었다는 뜻**이라 그 자체가 문제다.

---

## 통과 조건 판정

| # | 조건 | 판정 |
|---|---|---|
| 1 | D1 차이 전부 ±1 이내 | **통과** (12개 중 10개 일치, 2개 −1) |
| 2 | FR-02 L1-b 감점 −5 탐지 | **통과** |
| 3 | L1-b 이중 처벌 없음 | **통과** — D1·D3·D4 전부 부여값과 일치, 민감정보 서술이 점수 근거로 인용되지 않음 |
| 4 | FR-01 페이킹 답안 D2·D4 상한 2점 | **통과** — D2 4→2, D4 5→2 |
| 5 | FR-02 페이킹 답안 D2·D3 상한 3점 | **부분 통과** — D3 4→3 작동, D2는 실질 하향 0 |

> **통과 조건이 D1만 검사하도록 설계되어 있어서, 그 아래에서 발견된 앵커 오류 5건이 통과 판정에 가려진다.** 결함 7 참조.

---

## 높음 — 파일럿 시행 전 해결

### 결함 1. FIN 페르소나에서 감점이 과잉 발동한다 · 앵커 부여값과 규칙이 모순

`fr-02-anchors.md` L1-a(FIN)는 감점 0 / 합계 6-20으로 적혀 있는데, 감점 패스가 −5를 걸어 **0/20**이 됐다.

- 근거: FIN 페르소나의 *"상반기 결산 수치는 아직 공시 전이다"* + 답안의 *"비용 집행 데이터를 AI에 넣어 보고서를 만들게 하겠습니다"*
- 이것이 [penalty-risk.md](../rubrics/penalty-risk.md)의 −5 예시 **"월간 결산 계정별 집행액 원본을 넣는다(공시 전)"** 와 거의 축자로 겹친다. **규칙 텍스트만 놓고 보면 채점자 쪽이 맞다.**

파급이 더 크다. FIN을 고른 답안 3개 중 **2개(L1-a −5, 페이킹 답안 −2)** 가 감점을 받았다. FIN은 "자료를 AI에 넣는다"고 쓰기만 하면 −5 예시에 걸리는 구조라, 감점이 역량이 아니라 **페르소나 선택의 함수**가 된다. `penalty-risk.md`는 감점 발생률을 조직 리포트의 RSK 축 보조 지표로 쓰라고 하는데, 그 지표가 응시자가 어느 페르소나를 골랐는지에 오염된다.

**→ 갈래 A (결정 필요)**

| 선택지 | 내용 | 대가 |
|---|---|---|
| ⓐ | 앵커 L1-a의 부여 감점을 0 → −5로 정정 (규칙이 맞고 앵커가 틀렸다) | FIN 선택자의 감점률이 구조적으로 높아지는 것을 수용 |
| ⓑ | `penalty-risk.md`의 −5 "공시 전 결산" 항목 적용 범위를 좁힌다 (규칙이 과잉이다) | 리스크 측정 강도가 낮아짐 |

### 결함 2. "20/20 만점" 앵커 2개가 실제로는 20점이 아니다

`fr-01-anchors.md` L5-b와 `fr-02-anchors.md` L5-b는 둘 다 합계 20/20으로 적혀 있으나, **두 답안의 지시문에 D2 요소 ⑤(톤/품질 기준 또는 예시)가 없다.**

- L5-a에는 `"요약 문장은 '설비코드 – 변화 – 확인 필요 사항' 순서로 써라"`가 있어 ⑤가 충족된다
- L5-b 두 개에는 대응하는 문장이 없다
- 루브릭상 5점 요건은 "5개 전부"이므로 D2는 최대 4점 → 합계 **19/20**

두 패스가 독립적으로 같은 결론을 냈다. **만점 앵커가 만점이 아니면 캘리브레이션의 상한 기준점이 무너진다.**

### 결함 3. FR-01 L3-b의 D3 부여 2점이 앵커 자신의 근거와 모순 (유일한 ±2 불일치)

앵커 해설: *"①(집계 수치) 1개. '이상한 것은 뺀다'는 방법이 아니므로 ② 불인정"*
답안 원문: **"집계 수치를 원본으로 대조하고, 제안 중 이상한 것은 빼겠습니다"**

앞절의 "원본으로 대조"가 ②(원자료 대조)를 명백히 충족한다. **③(실패 시 행동)을 불인정하는 근거를 ②에 잘못 붙였다.** 채점자는 ①②③ = 3요소로 보고 4점을 냈다.

프로토콜상 2점 이상 불일치는 인간 판정 채택 + 앵커 보강 대상이다.

---

## 중간

### 결함 4. FR-02 L3-a·L3-b의 D3 부여 4점이 루브릭 카운트표와 어긋난다

두 앵커의 해설이 스스로 *"①② 2요소 + 환각 점검"* 이라 적고 4점을 부여했다. 카운트표상 **2개 요소 = 3점**이다.

"환각 점검"은 별도 요소가 아니라 ①의 강화이고, 루브릭은 그 경우 *"그 자체로 최소 3점"* 이라고만 규정한다 — 4점의 근거가 없다. 두 답안 모두 ③(실패 시 행동)과 ④(주체·시점)가 실제로 없다.

### 결함 5. "최대 3점"을 "= 3점"으로 적용했다 · 루브릭 2점 칸에 공백

D1에서 −1이 난 2건은 양쪽 문항의 L3-b이고 원인이 같다.

- 겉보기 human-in-the-loop 규칙은 *"이 구조만 있으면 **최대** 3점"* 인데, 앵커 작성자가 이를 3점 부여로 읽었다. **상한은 하한이 아니다.**
- 두 답안 모두 위임 기준 서술이 **아예 없어서** 채점자는 2점으로 내렸다.
- 그런데 루브릭 2점 칸은 *"구분은 있으나 기준이 '쉬운 일/어려운 일'"* — 즉 **기준이 작업량일 때**를 규정하고, **기준이 아예 없는 답안이 들어갈 칸이 없다.**

### 결함 6. 페이킹 방어가 절반만 작동한다

FR-01에 `required_anchors`(업무명·시스템명·데이터 항목명)를 하나도 쓰지 않은 답안을 넣었다.

| 차원 | 상한 없을 때 | 최종 | 상한 |
|---|---|---|---|
| D1 | 5 | **5** | 없음 |
| D2 | 4 | **2** | 있음 ✅ |
| D3 | 5 | **5** | 없음 |
| D4 | 5 | **2** | 있음 ✅ |
| | | **14/20** | |

상한은 설계대로 정확히 걸렸고 채점자가 적용 사실을 명시했다. **그런데 상한이 D2·D4에만 있어서 합계가 14/20이다.** 실제 업무 내용이 한 글자도 없는 답안이 70%를 받는다. [items/fr/FR-01.md](../items/fr/FR-01.md) 설계 메모의 *"AI 작성을 막는 것이 아니라 쓸모없게 만드는 장치"* 라는 주장과 어긋난다.

FR-02까지 범용 답안으로 내면 주관식 40점 만점에 **23점**이다.

**→ 갈래 B (결정 필요)**: D1·D3에도 상한을 걸 것인가. 루브릭 구조 변경이라 파급이 크다. 14/20을 수용 가능한 수준으로 볼지가 먼저다.

### 결함 7. 캘리브레이션 통과 조건이 D1만 검사해서 결함 2·3·4를 통과시킨다

[llm-scorer-prompt.md](../rubrics/llm-scorer-prompt.md)와 앵커 파일의 통과 조건은 "D1 차이가 모두 ±1 이내"뿐이다. C0은 D1이 전부 통과했고, D2·D3에서 발견된 앵커 오류 5건은 판정에 반영되지 않았다. **통과 조건이 가장 신뢰도가 높은 차원 하나만 보고 있다.**

### 결함 8. 프로토콜에 순환 참조가 있다

판단형(D1·D4) 프롬프트 템플릿은 앵커 6개를 프롬프트에 넣고 *"가장 가까운 앵커를 지목하라"* 고 요구한다. 그런데 **1단계 캘리브레이션에서는 채점 대상이 앵커 자신**이라 자기 자신을 대조하게 되고 부여 점수가 그대로 노출된다. 이 회차의 예외 규정이 없다.

---

## 낮음

### 결함 9. `04-scoring.md`의 IRR 위험도 표가 실측과 반대다

| 차원 | 표의 IRR 위험 | C0 실측 불일치 |
|---|---|---|
| D1 | **중간** — 앵커 필수 | 2 / 12 |
| D2 | 낮음 | **6 / 12** (전부 한 방향) |
| D3 | 낮음 | 3 / 12 |
| D4 | 낮음 | 1 / 12 |

**D2가 가장 수렴이 안 되는 차원이다.** 불일치 6건이 전부 요소 ①(수신자·용도)과 ⑤(톤/품질 기준)에서 발생했고, 방향이 전부 같다(채점자가 앵커 작성자보다 엄격). 두 요소의 인정 기준이 모호한 것이 원인으로 보인다.

### 결함 10. FR-01 L1-a의 D4 부여 1점

답안에 *"시간이 많이 줄어들 것 같습니다"* 가 있어 2점 칸(시간 절감만)에 해당한다. 부여 1점은 *"효율화될 것 같습니다"* 문장만 보고 매긴 것으로 보인다.

---

## 처리 계획

## 처리 결과

| 결함 | 결정 | 반영 |
|---|---|---|
| **1** FIN 감점 과잉 | 갈래 A — **앵커를 고친다** | `fr-02-anchors.md` L1-a 감점 0 → −5, 합계 6 → 1. 페르소나별 감점률을 파일럿에서 따로 집계하도록 주의 문구 추가 |
| **6** 페이킹 방어 절반 | 갈래 B — **네 차원 전부 상한** | `fr-01.md`·`fr-02.md` 공통 원칙, D1·D3·D4 절, `FR-01.md` 설계 메모, 채점 프롬프트 갱신. 범용 답안 최대치 14/20 → 8/20 |
| **11** 지문 상한 | 갈래 C — **상한 유지, 검사 추가** | 1차 보고가 측정 버그였음이 드러나 상한 변경은 철회. `lint_items.py`에 지문 길이·`chars` 정합·`stem_ref` 무결성 검사 추가 |
| **2** L5-b 만점 아님 | 기계적 정정 | 두 파일 L5-b D2 5 → 4, 합계 20 → 19 |
| **3** FR-01 L3-b D3 | 기계적 정정 | D3 2 → 3 (② 원자료 대조를 잘못 불인정했던 것) |
| **4** FR-02 L3-a·b D3 | 기계적 정정 | 각각 4 → 3 (환각 점검은 별도 요소가 아니라 ①의 강화) |
| **5** 상한을 하한으로 오해 | 기계적 정정 + 루브릭 보완 | 두 파일 L3-b D1 3 → 2. D1 2점 칸에 "기준을 아예 밝히지 않았다"를 추가 |
| **7** 통과 조건이 D1만 | 프로토콜 수정 | 통과 조건을 **네 차원 전부 ±1**로 확대 |
| **8** 순환 참조 | 프로토콜 수정 | 캘리브레이션 회차에는 판단형 패스에 앵커를 주지 않는다는 예외 규정 신설 |
| **9** IRR 위험도 오판 | 문서 수정 | `04-scoring.md` IRR 표를 실측값으로 교체. D2가 최저 수렴 |
| **10** L1-a D4 | 기계적 정정 | D4 1 → 2, 합계 4 → 5 |
| **13** 주관식 구상 시간 | 관찰 대상 | 상한 초과가 아니라 여유 부족. 파일럿에서 미완성률 관찰 |

### 정정 후 앵커 부여값

| | L1-a | L1-b | L3-a | L3-b | L5-a | L5-b |
|---|---|---|---|---|---|---|
| **FR-01** D1 / 합계 | 1 / 5 | 2 / 7 | 3 / 14 | 2 / 11 | 5 / 20 | 5 / 19 |
| **FR-02** D1 / 합계 | 1 / 1 | 2 / 2 | 3 / 13 | 2 / 13 | 5 / 20 | 5 / 19 |

### 남은 과제

1. **회차 C1 재실행** — 정정된 앵커로 캘리브레이션을 다시 돌려 네 차원 전부 ±1을 확인한다. 이번에도 같은 모델 계열이면 독립 검증이 아니므로, 가능하면 **다른 계열 모델이나 사람**을 붙인다
2. **앵커 세트의 공백 보강** — 겉보기 HITL 상한이 **실제로 결합하는** 사례(4점 요건을 채우면서 선택지 집합을 AI가 정하는 답안)와 **D1 4점** 앵커가 양쪽 파일 모두 없다. 페르소나 조건 미사용 상한 사례도 없다. 파일럿 응답에서 뽑아 보강한다
3. **소요 시간 실측**

---

# 부록 — 시간 예산 정합성 검사 (2026-09-16)

소요 시간 **실측은 사람이 해야 한다.** 대신 각 문항에 배정된 `time_sec`이 실제 글자 부피와 맞는지를 기계적으로 검사했다.

## 정정 기록 — 1차 검사는 틀렸다

**1차 검사에서 결함 11(지문 상한 초과)과 결함 12(지문 읽기 속도 526자/분)를 보고했으나 둘 다 무효다.**

원인은 측정 스크립트의 추출 버그다. 공통 지문 파일의 섹션 헤더는 `## 지문`인데 스크립트가 객관식 문항용 `## 상황`을 찾았고, 매치에 실패하자 **파일 전체(`## 설계 메모` 포함)로 폴백**해 글자수가 약 2배로 부풀었다.

| 지문 | 1차 보고(오류) | 실제 (`## 지문` 절만) | 상한 200자 |
|---|---|---|---|
| STEM-001 | 306자 | **152자** | OK |
| STEM-002 | 273자 | **150자** | OK |
| STEM-003 | 243자 | **136자** | OK |
| STEM-004 | 230자 | **124자** | OK |

**공통 지문 4개 전부 상한 안에 있다.** 지문 읽기도 426자(공백·기호 제외) / 120초 = **213자/분**으로 여유가 있다.

> **결함 11·12는 철회한다.** 이 정정 기록을 남기는 이유는, 같은 폴백 버그가 다른 검사에서도 조용히 통과할 수 있기 때문이다. 섹션을 못 찾으면 **폴백하지 말고 실패해야 한다.**

## 결함 11(정정) — 린트가 공통 지문 길이를 검사하지 않았다

상한 초과는 없었지만, **상한이 [AGENTS.md](../AGENTS.md)·[03-item-writing-rules.md](03-item-writing-rules.md)에만 있고 기계에는 없었던 것은 사실이다.** 지금 초과하지 않는다는 것과 앞으로 초과해도 안 잡힌다는 것은 다르다.

`scripts/lint_items.py`에 세 가지를 추가했다.

| 검사 | 기준 |
|---|---|
| 공통 지문 길이 | ≤ 200자 (`## 지문` 절, 실패) |
| `chars` frontmatter 정합 | 실제 길이와 ±10자 (경고) |
| `stem_ref` 참조 무결성 | 가리키는 STEM 파일이 존재 (실패) |

**상한 200자는 그대로 둔다.** 올릴 이유가 없었다.

## 결함 13 — 주관식 13분에 구상 시간이 3분뿐이다 (유효)

| 구분 | 부피 |
|---|---|
| FR-01 읽기 | 520자 |
| FR-02 읽기 | 690자 + 페르소나 200자 |
| 쓰기 | 800자 (권장 하한 400자 × 2) |

읽기 1,410자를 300자/분으로 읽으면 4.7분, 쓰기 800자를 150자/분(생각하며 쓰는 속도)으로 쓰면 5.3분이다. **합이 10분이고 배정은 13분이므로 두 문항을 합쳐 구상에 쓸 수 있는 시간이 3분이다.**

FR-01·FR-02 모두 ②에서 **실제로 입력할 지시문 문장**을 쓰게 한다. 지시문은 구상 없이 나오지 않는다. 다만 이것은 상한 초과가 아니라 **여유 부족**이므로, 파일럿에서 주관식 미완성률을 관찰한 뒤 판단한다.

## 정정된 전체 추정

| 구간 | 배정 | 추정 |
|---|---|---|
| 안내·자기보고 | 2.0분 | 2.0분 |
| 객관식 | 20.0분 | 17.1~20.4분 |
| 주관식 | 13.0분 | 10.0분(최소) ~ 13분 |
| **합계** | **35.0분** | **32~35분** |

객관식 3,616자를 300자/분으로 읽으면 12.1분이고, 남는 7.9분이 문항당 약 24초의 판단 시간이 된다. **배정이 부피에 대해 정합적이다.**

> 이것은 **추정이지 측정이 아니다.** 읽기 300자/분, 쓰기 150자/분, 문항당 판단 15~25초는 가정값이고 개인차가 크다. 실측이 이 범위 안에 들어오는지를 사람이 확인해야 한다.

## 파일럿에서 함께 볼 것

1. **주관식 미완성률** — ②의 지시문을 실제 문장으로 쓰지 못하고 설명으로 대체한 비율(결함 13의 실증)
2. **페르소나별 감점률** — FIN 편향이 실재하는지(결함 1)
3. **구간별 잔여 시간** — 응시자에게 1부·2부 종료 시각을 적게 한다

---

# 회차 C1 — 정정 앵커 재검증 (2026-09-16)

**채점 모델을 바꿨다.** 앵커를 쓴 것은 Opus, 채점은 **Sonnet**이다. C0의 가장 큰 한계 — 같은 모델이 자기가 쓴 답을 맞히는 것 — 를 줄인 것이다. 배치도 새로 섞었다.

집계표는 [pilot/item-analysis.md](../pilot/item-analysis.md) "회차 C1".

## 통과 판정 — 전부 통과

| 조건 | 결과 |
|---|---|
| **네 차원 전부 ±1 이내** (C0에서 강화한 새 조건) | **통과** — 60개 차원 점수 중 46개 완전 일치, 14개 ±1, **±2 이상 0개** |
| FR-02 L1-a·L1-b 감점 −5 탐지 | **통과** |
| FR-01 페이킹 답안 네 차원 상한 2점 | **통과** — 합계 **8/20** (C0에서는 14/20) |
| FR-02 페이킹 답안 네 차원 상한 3점 | **통과** |

**C0의 ±2 불일치 2건이 정정 후 전부 해소됐다.** C0에서 통과 조건이 D1만 보던 탓에 가려졌던 오류들이, 정정 후 다른 모델로 다시 재면 사라진다는 뜻이다.

## 정정값의 독립 재현

C0에서 제가 고친 값을 **다른 모델이 같은 값으로, 같은 근거로** 재현했는지가 이 회차의 핵심 질문이다.

| 결함 | 정정 내용 | C1 재현 |
|---|---|---|
| 1 | FR-02 L1-a 감점 0 → −5 | ✅ 근거도 "공시 전 경고 무시"로 동일 |
| 2 | 두 L5-b D2 5 → 4 | ✅ 근거도 "요소 ⑤ 없음"으로 동일 |
| 3 | FR-01 L3-b D3 2 → 3 | ✅ |
| 4 | FR-02 L3-a·L3-b D3 4 → 3 | ✅ |
| 5 | 두 L3-b D1 3 → 2 | ✅ 근거도 "상한이지 하한이 아님"으로 동일 |
| 6 | 페이킹 상한을 네 차원으로 | ✅ P5가 정확히 8/20 |
| 10 | FR-01 L1-a D4 1 → 2 | ❌ 1점으로 되돌아감 (결함 15) |

**갈래 A·B 결정이 둘 다 독립 검증을 통과했다.**

특히 페이킹 방어는 대조쌍이 깨끗하다. P4(L5-a, 실제 만점 앵커)와 P5(페이킹)는 **골격이 거의 같고 고유명사 유무만 다른데**, P4는 20/20, P5는 8/20이다. C1 채점자가 남긴 소견 그대로다 — *"P5는 P4와 내용 구조가 거의 동일하지만 담당 업무명·사내 시스템명·데이터 항목명이 전혀 없어 5점에서 2점으로 캡됨."*

---

## 새로 나온 결함

### 결함 14 [중간] — D2 요소 ①(수신자·용도)의 인정 기준이 모호하다

C1의 ±1 불일치 4건이 **전부 요소 ① 판정**에서 났고, **방향이 전부 같다**(채점자가 부여값보다 엄격).

| 배치 | 앵커 | 부여 | C1 | 쟁점 |
|---|---|---|---|---|
| P3 | FR-01 L3-a | 4 | 3 | 지시문에 수신자가 없음 |
| P6 | FR-01 L1-b | 2 | 1 | "팀장 승인"을 수신자로 볼 것인가 |
| Q4 | FR-02 L1-a | 2 | 1 | 수신자 없음 + 범위 미특정 |
| Q7 | FR-02 L1-b | 2 | 1 | 지시문에 "영업팀장" 없음 |

C0에서도 −1 6건이 ①·⑤에 몰렸고 방향이 같았다. **서로 다른 모델이 두 회차에 걸쳐 같은 자리에서 같은 방향으로 어긋난다.** 프롬프트 문제가 아니라 루브릭 문구 문제다.

핵심 쟁점: FR-01의 ① 인정 기준은 *"누가 읽고 무엇에 쓰는지"* 인데, **그것이 지시문 안에 있어야 하는지 답안 본문 어디에 있어도 되는지**가 정해져 있지 않다. FR-02는 *"페르소나 상황의 실제 수신자가 지시문에 들어 있다"* 로 위치를 못박았고, 실제로 FR-02 쪽 판정이 더 일관됐다.

**제안**: FR-01 ①의 인정 기준을 FR-02와 같은 형태로 통일하고, "본문에만 있고 지시문에 없으면 인정하지 않는다"를 명시한다. 이것은 루브릭 변경이므로 승인이 필요하다.

### 결함 15 [중간] — D4 2점 칸이 수치를 요구하는지 불명확하다

FR-01 L1-a의 D4가 **C0에서 2점, C1에서 1점**으로 갈렸다. 결함 10으로 1 → 2로 올렸던 것이 되돌아온 것이다.

- 답안: *"시간이 많이 줄어들 것 같습니다"*
- 2점 칸: *"**시간 절감만** 말한다 ("주 3시간 단축")"*

예시에는 수치가 있는데 **규칙 문장에는 수치 요구가 없다.** "많이"라는 형용사만으로 2점인지가 정해져 있지 않다. 공통 원칙은 *"형용사가 아니라 수치·고유명사·예시가 있다"* 를 구체성의 정의로 쓰므로, 그 원칙을 따르면 1점이 맞다.

**제안**: 2점 칸을 *"시간 절감만 말한다. 수치가 없는 '많이 줄어든다'류는 1점"* 으로 명확히 한다. 그 경우 결함 10의 정정을 되돌려 L1-a D4를 다시 1점으로 한다.

### 결함 16 [높음] — FIN 페르소나 감점 편향이 재확인됐고 더 나빠졌다

갈래 A 결정(앵커를 고친다)은 옳았으나, **그 결정이 드러낸 구조적 편향은 그대로다.**

| 회차 | FIN 답안 | 감점 |
|---|---|---|
| C0 | 2개 | −5 1건, −2 1건 (**2/2**) |
| C1 | 2개 | −5 2건 (**2/2**) |

두 회차, 두 모델에서 **FIN을 고른 답안은 예외 없이 감점**을 받았다. 더 나쁜 것은 **페이킹 답안(Q3)이 −5를 받았다**는 점이다. 이 답안은 `계정 과목`도 `집행 금액`도 언급하지 않고 "아래 자료를 바탕으로"라고만 쓴 완전 범용 답안인데, **페르소나 지문에 "공시 전"이 있다는 이유만으로** −5가 발동했다.

즉 **감점이 응답자가 제안한 행위의 구체성이 아니라 페르소나가 가진 조건에 의해 발동한다.** 같은 답안을 MFG나 STAFF로 내면 0점이다.

또한 같은 답안(Q3)에 대해 **C0은 −2, C1은 −5**로 모델 간 3점 차이가 났다. 감점 규칙이 경계 사례에서 신뢰도가 낮다.

`penalty-risk.md`는 감점 발생률을 조직 리포트의 RSK 축 보조 지표로 쓰라고 한다. **지금 상태로는 그 지표가 역량이 아니라 페르소나 선택 분포를 측정한다.**

**선택지**:

| | 내용 | 대가 |
|---|---|---|
| ⓐ | 감점 발생률을 **페르소나별로 분리 보고**하고 전체 비율은 쓰지 않는다 | 페르소나당 n이 작아 해석력이 떨어진다 |
| ⓑ | −5 발동 요건에 **"원천데이터 항목을 특정해 투입하겠다고 명시한 경우"** 를 추가한다. 범용 답안은 −2로 내려간다 | 리스크 측정 강도가 낮아진다 |
| ⓒ | FIN 페르소나의 "공시 전" 경고를 다른 5종과 같은 강도로 낮춘다 | FIN의 민감정보 측정 장치가 약해진다 |

**결정이 필요하다.**

---

## C1 이후 남은 과제

1. **결함 14·15·16 결정** — 전부 루브릭·규칙 변경이라 승인이 필요하다
2. **앵커 세트 공백 보강** — 아래 세 유형이 양쪽 파일 모두 없다
   - 겉보기 HITL 상한이 **실제로 결합하는** 사례(4점 요건을 채우면서 선택지 집합을 AI가 정하는 답안)
   - **D1 4점** 앵커
   - FR-02의 **페르소나 조건 미사용 상한** 사례
3. **소요 시간 실측** — 사람이 타이머를 켜고 응시해야 한다
4. **스폰서 서명** — 파일럿 시행의 선행 조건

## C1의 한계 (여전히 남는 것)

- **교차 벤더 검증이 아니다.** Opus와 Sonnet은 다른 모델이지만 같은 Claude 계열이고 학습 데이터·정렬 방식이 겹친다. 두 모델이 같은 편향을 공유하면 일치해도 타당도의 증거가 되지 않는다
- **인간 채점자가 없다.** 실제 시행의 이중 채점(LLM 전수 + 인간 20% 표본)은 아직 한 번도 돌려보지 않았다
- **temperature 0을 설정하지 못했다**

---

# 앵커 세트 공백 보강 (2026-09-16)

C1이 확인한 공백 3종을 메우려고 신규 앵커 후보 3종을 썼다. **점수를 비운 채, 값이 확정된 앵커 2개를 대조군으로 섞어, 다른 모델(Sonnet)에 먼저 채점시켰다.** 답안을 쓴 쪽이 점수까지 매기면 그 앵커는 검증된 것이 아니다 — 회차 C0에서 "만점 앵커가 만점이 아니었던" 원인이 정확히 그것이다.

**대조군 2개가 다섯 패스 전부에서 확정값과 일치**했으므로 이 회차는 캘리브레이션되어 있다.

| 후보 | 메우려는 공백 | 의도 | 채점 결과 | 판정 |
|---|---|---|---|---|
| FR-01 물류 지연 | D1 4점 레벨 | 4 / 4 / 5 / 4 / 0 = 17 | **동일** | **채택 → `L4-a`** |
| FR-01 소모품 발주 | 겉보기 HITL 상한이 결합하는 사례 | 3(base 4) / 4 / 4 / 4 / 0 = 15 | **동일** | **채택 → `L3-c`** |
| FR-02 MFG 범용 템플릿 | 페르소나 조건 미사용 상한 | 3 / 3 / 3 / 2 / 0 = 11 | 3 / 3 / **4** / 2 / 0 = 12 | **폐기** |

## 채택 2종이 메운 것

**`L3-c`** — 세트에서 **겉보기 HITL 상한이 실제로 점수를 바꾸는 유일한 사례**다. 채점자가 독립적으로 같은 판정을 냈다.

> *"겉보기 human-in-the-loop 감별: **해당함.** 'AI에게 발주 후보 품목 5개와 뽑게 하고, 저는 그중에서 타당한 것을 골라'는 정확히 규정된 패턴 — 선택지 집합 자체를 AI가 정한 구조. **상한 3점 적용.** base = 4, 상한 적용 → 최종 = 3."*

기존 `L3-b`는 위임 기준이 아예 없어 상한 없이도 2점이므로 상한이 결합할 일이 없었다. 회차 C0에서 "최대 3점"을 "= 3점"으로 오해한 오류(결함 5)가 이 공백에서 나왔다.

**`L4-a`** — D1 4점 레벨. 정정 전 분포는 1·2·3·2·5·5로 **4점이 비어 있었다.** `L5-a`·`L5-b`와 차이가 "경계가 흔들리는 예외 상황과 처리" 하나뿐이라 4점과 5점의 경계를 보여주는 최소 대조쌍이 된다.

## 결함 17 [높음] — 페르소나 조건 미사용 상한의 발동 조건이 모호하다

폐기한 후보가 드러낸 문제다. **같은 답안·같은 규칙인데 패스마다 상한 적용 판정이 갈렸다.**

| 패스 | 판정 | 근거 |
|---|---|---|
| D2 | **적용** | "페르소나 고유 사실(시스템명/기간/항목명/공장장) 전무 → 상한 3점" |
| D3 | **미적용** | "'자료에 없는 내용이 들어갔는지'는 페르소나의 환각 함정을 겨냥 → 페르소나 조건 실사용" |
| D1 | 유보 | "상한 3점(해당 시에도 base와 동일)" |
| D4 | 유보 | "base < cap이라 불필요(다만 페르소나 실사용 매우 빈약)" |

게다가 **같은 답안을 다른 페르소나로 내면 판정이 뒤집힌다.** 회차 C1에서 이 답안은 FIN 페르소나로 제출됐고 그때 D3에 상한이 적용됐다(4→3). MFG로 바꾸자 적용되지 않았다.

원인은 상한 조건의 문구다. 루브릭은 *"페르소나 지문에 주어진 고유명사·데이터 항목·제약을 답안이 실제로 사용하는지"* 를 본다고 하는데, **"자료에 없는 내용은 만들지 마라" 같은 범용 환각 차단 문구를 '제약 사용'으로 볼지가 정해져 있지 않다.** 이 문구는 어느 페르소나에도 붙일 수 있으므로 오히려 템플릿 답안의 특징인데, 지금 규칙으로는 상한을 무력화하는 데 쓰인다.

> **실질적 위험**: 범용 LLM 답안이 **"자료에 없는 내용은 쓰지 마라" 한 줄만 넣으면 페이킹 방어를 우회할 수 있다.** 갈래 B로 상한 범위는 네 차원으로 넓혔지만, 상한이 *언제 켜지는지*는 여전히 불안정하다.

**해법의 단서는 같은 루브릭 안에 있다.** FR-02 D4의 게이트는 *"3점 이상은 페르소나 지문에 등장한 **실제 단계 이름**을 지목해야 한다"* 로 쓰여 있고, **네 패스 전부가 이 조건을 일관되게 적용했다.** 위치와 대상을 못박은 문구는 흔들리지 않았다.

**제안**: 상한 조건을 *"페르소나 지문에 등장한 **고유명사 또는 데이터 항목명을 최소 1개 지목**해야 한다. 어느 페르소나에도 적용 가능한 일반 서술(예: '자료에 없는 내용은 쓰지 마라')은 지목으로 인정하지 않는다"* 로 바꾼다. **루브릭 변경이므로 승인이 필요하다.**

## 남은 공백 (FR-02)

FR-02는 세 공백이 그대로다 — 겉보기 HITL 상한 결합 사례 / D1 4점 / 페르소나 조건 미사용 상한 사례.

마지막 항목은 **결함 17을 해결하기 전에는 앵커를 만들 수 없다.** 기준이 흔들리는 조건으로 기준점을 만들 수는 없다. 앞의 둘은 파일럿 응답에서 뽑는 편이 낫다 — 합성 앵커보다 실제 응답이 낫고, 파일럿은 어차피 이 유형을 다수 산출할 것이다.

## 부수 발견 — `L5-a`의 리스크 처리가 명시적이지 않다

감점 패스가 `L5-a`(FR-01 만점 앵커)를 **"애매 시 무감점 원칙"** 으로 0 처리했다. 내려받는 항목에 `작업자`가 있는데 지시문에서 제외를 명시하지 않았기 때문이다. 판정 자체는 맞지만, **만점 앵커가 감점 판정에서 "애매"로 분류되는 것은 기준점으로 부적절하다.** `L5-b`는 `반려 사유 코드` 제약을 명시해 "정상적 리스크 처리"로 분류됐다. `L5-a`의 지시문에 한 줄을 넣어 맞추는 것을 검토한다(루브릭이 아니라 앵커 본문 수정이므로 파급은 작다).

---

# 루브릭 결정 (2026-09-17)

[12-open-issues.md](12-open-issues.md) A절의 결정 대기 항목 5건을 정했다. 결정은 문답으로 따졌다 — "이 규칙이 재려는 것은 무엇인가 → 지금 문구는 그것을 재는가 → 고치면 무엇을 잃는가". **결정마다 기대 점수를 먼저 적어 두고**, 점수와 라벨을 지운 채 순서를 섞은 답안을 다른 모델(Sonnet)에 차원별 독립 패스로 채점시켜 대조했다.

## 결정

| 항목 | 물음 | 결정 | 버린 안 |
|---|---|---|---|
| **S0-1** FR-02 ③이 D3 5점 요건을 노출 | 문항이 공개해도 되는 것은 어디까지인가? FR-01 설계 메모는 "변별은 4~5점에서 나므로 차원을 공개해도 손해가 없다"고 했다. 그렇다면 5점 요건을 묻는 순간 그 전제가 깨진다 | **FR-02 ③에서 "무엇을 먼저 보고 무엇은 보지 않겠습니까?"를 뺐다.** 원칙: **질문은 4점 요건까지만 묻는다.** 점검해 보니 D1 예외·D2 ⑤·D4 성과 지표는 원래 묻지 않았고, D3 우선순위만 FR-02에서 묻고 있었다 | FR-01에도 같은 질문을 넣어 대칭 맞추기 — 5점이 질문 따라 쓰기를 재게 된다 |
| **결함 17 · S0-3** 페르소나 상한 발동이 불안정 | 상한이 막으려는 것은 무엇인가? 페르소나를 읽지 않고 쓴 답안이다. 그 증거는 "제약을 썼는가"가 아니라 **이 페르소나에만 있는 이름을 썼는가**다 | 판정을 **페르소나별 `anchor_items`(고유 데이터 항목명) 등장 여부로 기계화**했다. `score.py --prompts`가 답안마다 판정을 적고 채점자는 뒤집지 않는다. `부서`·`담당자`·`서비스`·`거래처` 같은 일반어는 목록에서 뺐다 — 범용 답안도 우연히 쓴다 | 지문의 `코드 표기` 항목 전부 — 일반어가 섞여 우회된다 / LLM 판정 유지 — 패스마다 갈린 것이 결함 17이다 |
| **결함 14 · S0-2** D2 ① 위치 모호, ⑤ 도달 불가 | AI는 무엇을 받는가? 지시문이다. 본문 ①에 수신자를 썼어도 AI는 모른다. ⑤가 0/20인 것은 문구가 막아서인가, 아무도 안 해서인가? S0 원문을 다시 읽으니 문장 틀·예시를 준 답안이 실제로 없었다 | **D2는 ② 지시 설계 항목과 첨부 프롬프트 안에서만 센다.** 승인자·검토자는 수신자가 아니다. **④는 문서 전체의 모양, ⑤는 문장·항목 하나의 기준이나 예시**로 경계를 적었다. ⑤ 자체는 유지 — 앵커 L5-a·FR-02 L5-a처럼 쓰는 사람은 쓴다 | 지시문만 센다 — FR-02 "설명만 하면 상한 3"과 모순 / ⑤ 삭제 — 5점이 쉬워져 D2가 4점 척도로 줄어든다 |
| **결함 15** D4 2점이 수치를 요구하는가 | D4의 사다리는 무엇을 재는가? 효과가 **어디까지 갔는지**(시간 → 단계 → 전환 → 확산)다. "많이 줄어든다"와 "주 3시간"은 같은 칸의 사람이고 처방도 같다 | **수치를 요구하지 않는다.** 1점은 무엇이 나아지는지 특정하지 않는 답("효율화된다")이다. L1-a D4 = 2 유지 | 결함 15 원 제안(수치 없으면 1점) — 공통 원칙 "존재하면 2점"과 충돌하고 처방이 갈리지 않는다 |
| **결함 16** FIN 감점 편향 | 감점은 무엇을 재는가? **응답자가 제안한 행위**다. 페이킹 답안 Q3는 무엇을 넣는지 말하지 않았는데 페르소나 경고가 빈칸을 채웠다 | **−5는 답안 글에서 민감 데이터가 식별될 때만**(이름 지목 또는 원본·전체 투입 명시). 일반 참조는 0. **FR-02 감점률은 페르소나별로 보고**한다. DEV·STAFF 지문에도 민감 데이터 경고가 있는데 `sensitive_data: false`였던 것을 true로 고쳤다 | 결함 16 ⓒ FIN 경고 약화 — 측정 장치를 버린다 / 일반 참조에 −2 — AI에 자료를 주겠다는 거의 모든 답이 감점되어 AI 기피를 보상한다 |

## 검증

사전 등록한 기대값과 독립 채점의 대조. 합성 T1·T2는 이번에 만든 FIN 대조쌍이다(T1 "아래 자료를 바탕으로", T2 "원본 표 전체를 그대로 붙여넣을게", 나머지 동일).

| 패스 | 대상 | 일치 | ±1 | ±2 이상 |
|---|---|---|---|---|
| 감점 | 앵커 2 · S0 6 · 합성 2 | **10/10** | 0 | 0 |
| D2 | 앵커 8 · S0 4 | 9/12 | 3 | 0 |
| D4 | 앵커 4 · S0 2 | 5/6 | 0 | 1 |

- **결정이 직접 바꾸는 점수는 전부 예측대로 나왔다.** FR-01 L1-b D2 1(승인자 ≠ 수신자), FR-01 L3-a D2 3(수신자가 지시문 밖), FR-01 L1-a D4 2(수치 없는 시간 절감), T1 감점 0 / T2 −5, S0 DESIGN·MKT −5 유지, OPS(FIN, 사내 도구 명시) 0.
- **감점 10건 중 L1-a·T1·DESIGN은 규칙 예시와 문구가 거의 같다.** 이 셋은 규칙을 읽었는지를 확인할 뿐이고, 일반화의 근거는 나머지 S0 5건이다.
- **D2 ±1 3건.** FR-02 L1-b는 회차 C1에 이어 **두 번째로 1점**이 나와 앵커를 2 → 1로 고쳤다("거래처별로 정리"는 ④가 아니다). S0 SALES(3, 기대 4: "이 표"만으로는 ② 불인정)·S0 DESIGN(3, 기대 2: "슬라이드로"를 ④ 형식으로 인정)은 기대값 쪽이 과했거나 모자랐다. 문구 문제의 신호로 보지 않는다.
- **D4 ±2 1건 — 새 결함 18.** S0 DESIGN FR-01 *"슬라이드 배치하는 시간이 좀 줄어드는 정도"*에 기대 2, 채점 4. 3점 칸은 *"사라지거나 **줄어드는** 구체적 작업 단계를 이름으로"* 인데 채점 시 주의의 2점 예시는 *"보고서 작성이 빨라진다"* 다. "X하는 시간이 줄어든다"가 어느 쪽인지 문구가 정하지 않는다. 이번 결정(1/2 경계)과 무관한 기존 모호성이고, 한 패스의 신호라 [12-open-issues.md](12-open-issues.md)에 올렸다.
- 앵커 정정: FR-01 L1-b 7 → 6, L3-a 14 → 13 / FR-02 L1-a 1 → 0(D2 산술 오류), L1-b 2 → 1.

## 함께 고친 사고 — 채점 패킷에서 FR-02 상한 규칙이 빠져 있었다

`score.py`의 `_scrub`은 채점자에게 회차 기록이 새지 않도록 **"결함 N"이 들어간 줄을 통째로 지운다.** 그런데 FR-02의 페르소나 상한 규칙 문장이 같은 줄 끝에 "결함 6" 인용을 달고 있어 **규칙째 지워졌다.** 회차 S0의 D1~D4 패킷(`pass-D2.md` 등)에 FR-02 상한 규칙이 없다. S0 답안은 모두 페르소나 항목명을 썼으므로 점수는 바뀌지 않지만, S0-3("상한이 한 번도 발동하지 않았다")은 **채점자가 규칙을 받지 못한 상태에서 나온 관찰**이다.

- 규칙 문장과 회차·결함 인용을 다른 줄로 나눴다
- `tests/test_score.py`에 **패킷에 상한 규칙이 실제로 실리는지** 검사를 넣고, 규칙 줄에 "결함 6"을 다시 붙여 FAIL을 확인한 뒤 원복했다

## 함께 넣은 검사

| 검사 | 막는 것 | 고의로 깨뜨린 확인 |
|---|---|---|
| 린트: 응시본 21·22번 ①~④의 물음 ↔ FR 문항 파일(양방향) | 이번처럼 문항만 고치고 지필본에 옛 질문이 남는 것. 기존 린트는 주관식 질문을 대조하지 않았다 | 지필본에 옛 ③ 질문 복원 → FAIL 2 / 문항 파일에서 ④ 물음 하나 삭제 → FAIL 1 |
| 린트: 페르소나 `anchor_items` ⊆ 지문의 `코드 표기` 항목 | 지문에 없는 이름을 기준으로 삼아 전원이 상한에 걸리는 것 | 이름 변조 → FAIL / 필드 삭제 → FAIL |
| 테스트: 상한 판정(띄어쓰기 무시, 범용 문구 무효, 타 페르소나 항목 무효), 미지 페르소나는 폴백 없이 실패 | 판정 로직 회귀 | 띄어쓰기 처리 제거 → FAIL 2 / 미지 페르소나 폴백 → ERROR |
