# 10. 캘리브레이션 결함 기록 — 회차 C0

[07-validity-plan.md](07-validity-plan.md) 2단(자가 응시와 채점 리허설)의 채점 리허설에서 발견된 결함이다.
집계표는 [pilot/item-analysis.md](../pilot/item-analysis.md) "회차 C0".

**전부 미수정이다.** 갈래 A·B는 진단 설계 판단이라 결정이 필요하고, 나머지는 결정 후 일괄 반영한다.

---

## 실행 조건

| 항목 | 값 |
|---|---|
| 일자 | 2026-09-16 |
| 대상 | 앵커 12개(FR-01 6 + FR-02 6) + 페이킹 시험 답안 2개 |
| 프로토콜 | [rubrics/llm-scorer-prompt.md](../rubrics/llm-scorer-prompt.md) — 차원별 독립 4패스 + 감점 1패스 |
| 독립성 확보 | 패스마다 별도 컨텍스트. 배치 순서 섞음, 레벨 라벨·부여 점수·출제자 볼드 강조 제거, 루브릭 외 파일 접근 차단 |

### 이 결과의 한계 (해석 전에 읽는다)

1. **독립 검증이 아니다.** 앵커와 부여 점수를 쓴 것과 같은 모델 계열이 채점했다. **일치 항목은 타당도의 증거가 아니다** — 같은 편향을 공유할 뿐이다. 반대로 **불일치 항목은 신호가 강하다**: 정답지 없이 다시 읽었을 때 어긋났다는 것은 앵커나 루브릭 자체가 모호하다는 뜻이다. 아래 결함은 전부 불일치에서 나왔다.
2. **temperature 0을 설정하지 못했다.** 결정론적 재현이 보장되지 않는다.
3. **D1·D4 패스에 앵커를 주지 않았다** (결함 7의 순환을 피하는 유일한 방법). 실제 파일럿 채점은 앵커를 받으므로 조건이 다르다.
4. **앵커의 볼드 강조를 제거했다.** 볼드는 출제자 주석이지 응답자 원문이 아니고 실제 응답에는 없다. 이것이 D2 불일치 일부를 만들었을 수 있는데, 그렇다면 **원래 부여 점수가 볼드 단서에 의존하고 있었다는 뜻**이라 그 자체가 문제다.

---

## 통과 조건 판정

| # | 조건 | 판정 |
|---|---|---|
| 1 | D1 차이 전부 ±1 이내 | **통과** (12개 중 10개 일치, 2개 −1) |
| 2 | FR-02 L1-b 감점 −5 탐지 | **통과** |
| 3 | L1-b 이중 처벌 없음 | **통과** — D1·D3·D4 전부 부여값과 일치, 민감정보 서술이 점수 근거로 인용되지 않음 |
| 4 | FR-01 페이킹 답안 D2·D4 상한 2점 | **통과** — D2 4→2, D4 5→2 |
| 5 | FR-02 페이킹 답안 D2·D3 상한 3점 | **부분 통과** — D3 4→3 작동, D2는 실질 하향 0 |

> **통과 조건이 D1만 검사하도록 설계되어 있어서, 그 아래에서 발견된 앵커 오류 5건이 통과 판정에 가려진다.** 결함 7 참조.

---

## 높음 — 파일럿 시행 전 해결

### 결함 1. FIN 페르소나에서 감점이 과잉 발동한다 · 앵커 부여값과 규칙이 모순

`fr-02-anchors.md` L1-a(FIN)는 감점 0 / 합계 6-20으로 적혀 있는데, 감점 패스가 −5를 걸어 **0/20**이 됐다.

- 근거: FIN 페르소나의 *"상반기 결산 수치는 아직 공시 전이다"* + 답안의 *"비용 집행 데이터를 AI에 넣어 보고서를 만들게 하겠습니다"*
- 이것이 [penalty-risk.md](../rubrics/penalty-risk.md)의 −5 예시 **"월간 결산 계정별 집행액 원본을 넣는다(공시 전)"** 와 거의 축자로 겹친다. **규칙 텍스트만 놓고 보면 채점자 쪽이 맞다.**

파급이 더 크다. FIN을 고른 답안 3개 중 **2개(L1-a −5, 페이킹 답안 −2)** 가 감점을 받았다. FIN은 "자료를 AI에 넣는다"고 쓰기만 하면 −5 예시에 걸리는 구조라, 감점이 역량이 아니라 **페르소나 선택의 함수**가 된다. `penalty-risk.md`는 감점 발생률을 조직 리포트의 RSK 축 보조 지표로 쓰라고 하는데, 그 지표가 응시자가 어느 페르소나를 골랐는지에 오염된다.

**→ 갈래 A (결정 필요)**

| 선택지 | 내용 | 대가 |
|---|---|---|
| ⓐ | 앵커 L1-a의 부여 감점을 0 → −5로 정정 (규칙이 맞고 앵커가 틀렸다) | FIN 선택자의 감점률이 구조적으로 높아지는 것을 수용 |
| ⓑ | `penalty-risk.md`의 −5 "공시 전 결산" 항목 적용 범위를 좁힌다 (규칙이 과잉이다) | 리스크 측정 강도가 낮아짐 |

### 결함 2. "20/20 만점" 앵커 2개가 실제로는 20점이 아니다

`fr-01-anchors.md` L5-b와 `fr-02-anchors.md` L5-b는 둘 다 합계 20/20으로 적혀 있으나, **두 답안의 지시문에 D2 요소 ⑤(톤/품질 기준 또는 예시)가 없다.**

- L5-a에는 `"요약 문장은 '설비코드 – 변화 – 확인 필요 사항' 순서로 써라"`가 있어 ⑤가 충족된다
- L5-b 두 개에는 대응하는 문장이 없다
- 루브릭상 5점 요건은 "5개 전부"이므로 D2는 최대 4점 → 합계 **19/20**

두 패스가 독립적으로 같은 결론을 냈다. **만점 앵커가 만점이 아니면 캘리브레이션의 상한 기준점이 무너진다.**

### 결함 3. FR-01 L3-b의 D3 부여 2점이 앵커 자신의 근거와 모순 (유일한 ±2 불일치)

앵커 해설: *"①(집계 수치) 1개. '이상한 것은 뺀다'는 방법이 아니므로 ② 불인정"*
답안 원문: **"집계 수치를 원본으로 대조하고, 제안 중 이상한 것은 빼겠습니다"**

앞절의 "원본으로 대조"가 ②(원자료 대조)를 명백히 충족한다. **③(실패 시 행동)을 불인정하는 근거를 ②에 잘못 붙였다.** 채점자는 ①②③ = 3요소로 보고 4점을 냈다.

프로토콜상 2점 이상 불일치는 인간 판정 채택 + 앵커 보강 대상이다.

---

## 중간

### 결함 4. FR-02 L3-a·L3-b의 D3 부여 4점이 루브릭 카운트표와 어긋난다

두 앵커의 해설이 스스로 *"①② 2요소 + 환각 점검"* 이라 적고 4점을 부여했다. 카운트표상 **2개 요소 = 3점**이다.

"환각 점검"은 별도 요소가 아니라 ①의 강화이고, 루브릭은 그 경우 *"그 자체로 최소 3점"* 이라고만 규정한다 — 4점의 근거가 없다. 두 답안 모두 ③(실패 시 행동)과 ④(주체·시점)가 실제로 없다.

### 결함 5. "최대 3점"을 "= 3점"으로 적용했다 · 루브릭 2점 칸에 공백

D1에서 −1이 난 2건은 양쪽 문항의 L3-b이고 원인이 같다.

- 겉보기 human-in-the-loop 규칙은 *"이 구조만 있으면 **최대** 3점"* 인데, 앵커 작성자가 이를 3점 부여로 읽었다. **상한은 하한이 아니다.**
- 두 답안 모두 위임 기준 서술이 **아예 없어서** 채점자는 2점으로 내렸다.
- 그런데 루브릭 2점 칸은 *"구분은 있으나 기준이 '쉬운 일/어려운 일'"* — 즉 **기준이 작업량일 때**를 규정하고, **기준이 아예 없는 답안이 들어갈 칸이 없다.**

### 결함 6. 페이킹 방어가 절반만 작동한다

FR-01에 `required_anchors`(업무명·시스템명·데이터 항목명)를 하나도 쓰지 않은 답안을 넣었다.

| 차원 | 상한 없을 때 | 최종 | 상한 |
|---|---|---|---|
| D1 | 5 | **5** | 없음 |
| D2 | 4 | **2** | 있음 ✅ |
| D3 | 5 | **5** | 없음 |
| D4 | 5 | **2** | 있음 ✅ |
| | | **14/20** | |

상한은 설계대로 정확히 걸렸고 채점자가 적용 사실을 명시했다. **그런데 상한이 D2·D4에만 있어서 합계가 14/20이다.** 실제 업무 내용이 한 글자도 없는 답안이 70%를 받는다. [items/fr/FR-01.md](../items/fr/FR-01.md) 설계 메모의 *"AI 작성을 막는 것이 아니라 쓸모없게 만드는 장치"* 라는 주장과 어긋난다.

FR-02까지 범용 답안으로 내면 주관식 40점 만점에 **23점**이다.

**→ 갈래 B (결정 필요)**: D1·D3에도 상한을 걸 것인가. 루브릭 구조 변경이라 파급이 크다. 14/20을 수용 가능한 수준으로 볼지가 먼저다.

### 결함 7. 캘리브레이션 통과 조건이 D1만 검사해서 결함 2·3·4를 통과시킨다

[llm-scorer-prompt.md](../rubrics/llm-scorer-prompt.md)와 앵커 파일의 통과 조건은 "D1 차이가 모두 ±1 이내"뿐이다. C0은 D1이 전부 통과했고, D2·D3에서 발견된 앵커 오류 5건은 판정에 반영되지 않았다. **통과 조건이 가장 신뢰도가 높은 차원 하나만 보고 있다.**

### 결함 8. 프로토콜에 순환 참조가 있다

판단형(D1·D4) 프롬프트 템플릿은 앵커 6개를 프롬프트에 넣고 *"가장 가까운 앵커를 지목하라"* 고 요구한다. 그런데 **1단계 캘리브레이션에서는 채점 대상이 앵커 자신**이라 자기 자신을 대조하게 되고 부여 점수가 그대로 노출된다. 이 회차의 예외 규정이 없다.

---

## 낮음

### 결함 9. `04-scoring.md`의 IRR 위험도 표가 실측과 반대다

| 차원 | 표의 IRR 위험 | C0 실측 불일치 |
|---|---|---|
| D1 | **중간** — 앵커 필수 | 2 / 12 |
| D2 | 낮음 | **6 / 12** (전부 한 방향) |
| D3 | 낮음 | 3 / 12 |
| D4 | 낮음 | 1 / 12 |

**D2가 가장 수렴이 안 되는 차원이다.** 불일치 6건이 전부 요소 ①(수신자·용도)과 ⑤(톤/품질 기준)에서 발생했고, 방향이 전부 같다(채점자가 앵커 작성자보다 엄격). 두 요소의 인정 기준이 모호한 것이 원인으로 보인다.

### 결함 10. FR-01 L1-a의 D4 부여 1점

답안에 *"시간이 많이 줄어들 것 같습니다"* 가 있어 2점 칸(시간 절감만)에 해당한다. 부여 1점은 *"효율화될 것 같습니다"* 문장만 보고 매긴 것으로 보인다.

---

## 처리 계획

| 결함 | 성격 | 처리 |
|---|---|---|
| 1 | 설계 판단 | **갈래 A 결정 대기** |
| 6 | 설계 판단 | **갈래 B 결정 대기** |
| 2, 3, 4, 5, 10 | 앵커 부여값을 루브릭에 맞추는 정정 | 결정 후 일괄 반영 |
| 7, 8, 9 | 프로토콜·문서 문구 수정 | 결정 후 일괄 반영 |

결함 5는 정정과 함께 **루브릭 D1 2점 칸의 문구 보완**(기준이 없는 경우를 포함)이 필요하다.

---

# 부록 — 시간 예산 정합성 검사 (2026-09-16)

소요 시간 **실측은 사람이 해야 한다.** 대신 각 문항에 배정된 `time_sec`이 그 문항의 실제 글자 부피와 맞는지를 기계적으로 검사했다. 어긋난 곳이 실측에서 시간을 잡아먹는 지점이다.

## 결함 11. 공통 지문 4개가 전부 글자수 상한을 초과하고, 린트가 이를 검사하지 않는다

[AGENTS.md](../AGENTS.md)의 상한표는 **공통 지문 200자**다. 실제는 —

| 지문 | 원문 길이 | 공백·기호 제외 | 상한 대비 |
|---|---|---|---|
| STEM-001 | 416자 | 306자 | **+53%** |
| STEM-002 | 385자 | 273자 | **+37%** |
| STEM-003 | 343자 | 243자 | **+22%** |
| STEM-004 | 324자 | 230자 | **+15%** |

**어떤 계산 기준을 쓰든 4개 전부 초과다.** `scripts/lint_items.py`는 단독 지문 120자만 경고하고 **공통 지문 길이는 검사하지 않는다.** 상한이 문서에만 있고 기계에는 없어서 드리프트를 아무도 못 잡았다.

> 상한을 고칠지 지문을 고칠지는 설계 판단이다. **지문을 얕게 깎으면 SJT의 전제인 상황 판단이 무너지고 금지하려던 툴 상식 문항과 구분되지 않는다**([99-changelog.md](99-changelog.md) 초기 설계 결정). 린트에 검사를 추가하면 현재 상태가 즉시 FAIL이 되므로, 결정 전에는 추가하지 않는다.

## 결함 12. 지문 읽기 배정 120초가 요구하는 속도가 비현실적이다

`forms/MANIFEST.yml`은 공통 지문 4개 읽기에 30초씩 총 120초를 배정한다. 지문 합계는 1,052자(공백 제외)이므로 **526자/분**이 필요하다.

한국어 성인의 묵독 속도는 쉬운 산문에서 대략 400~600자/분이지만, 이 지문은 **3개 문항에 답하기 위해 상황을 기억에 붙잡아 둬야 하는 판단용 지문**이다. 판단을 위한 독해는 통상 250~350자/분이다. 같은 기준이면 지문 읽기에 **약 210초(3.5분)** 가 필요하고, 배정보다 90초 부족하다.

## 결함 13. 주관식 13분에 구상 시간이 사실상 없다

| 구분 | 부피 |
|---|---|
| FR-01 읽기 | 520자 |
| FR-02 읽기 | 690자 + 페르소나 200자 |
| 쓰기 | 800자 (권장 하한 400자 × 2) |

읽기 1,410자를 300자/분으로 읽으면 4.7분, 쓰기 800자를 150자/분(생각하며 쓰는 속도)으로 쓰면 5.3분이다. **합이 10분이고 배정은 13분이므로, 두 문항을 합쳐 구상에 쓸 수 있는 시간이 3분이다.**

FR-01·FR-02 모두 ②에서 **실제로 입력할 지시문 문장**을 쓰게 한다. 지시문은 구상 없이 나오지 않는다.

## 전체 추정치

| 구간 | 배정 | 추정 (읽기 300자/분 기준) |
|---|---|---|
| 안내·자기보고 | 2.0분 | 2.0분 |
| 객관식 | 20.0분 | 19~22분 (문항당 판단 15~25초 가정) |
| 주관식 | 13.0분 | 13~16분 |
| **합계** | **35.0분** | **34~40분** |

**하한은 목표 안에 들어오지만 상한이 35분 ±3분(32~38분)을 벗어난다.** 병목은 글자수 상한을 넘긴 지문(결함 11)과 주관식 구상 시간(결함 13)이다.

> 이것은 **추정이지 측정이 아니다.** 읽기 속도 300자/분, 문항당 판단 15~25초는 가정값이고 개인차가 크다. 실측이 이 범위 안에 들어오는지를 사람이 확인해야 한다.

## 처방 후보 (결정 필요)

문항을 공통 지문 블록으로 옮기면 지문 읽기 비용이 더 많은 문항에 분산되고 문항당 `time_sec`도 70초에서 40초로 내려간다 — 설계가 정한 레버다. **다만 지금은 쓸 수 없다**: 린트가 블록당 문항 수를 정확히 3개로 강제한다(`len(axs) != 3` → FAIL). 블록을 4문항으로 늘리거나 5번째 블록을 만들어야 하고, 둘 다 배분표 개정이 따른다.
