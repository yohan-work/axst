# HANDOFF — 작업 인계 (2026-09-17, Claude Code → Codex CLI)

이 파일은 **이어서 작업할 에이전트가 가장 먼저 읽는 문서**다. 레포 규약 정본은 [AGENTS.md](AGENTS.md)이고, 이 파일은 "지금 어디까지 왔고 다음에 무엇을 하는가"만 다룬다.

---

## 한 줄 요약

AX Literacy 진단(기업 구성원의 AI 활용 역량 35분 진단)의 **문항 뱅크 · 채점기 · 온라인 응시본 · 검증 3회차가 끝났다.** 남은 일은 (1) 채점기 버그 1건, (2) 사용자 결정이 필요한 루브릭 결함들, (3) 사람이 해야 하는 실측·서명·실제 파일럿이다.

## 먼저 읽을 것 (순서대로)

1. [AGENTS.md](AGENTS.md) — 규약, 불가침 규칙, 커밋 전 절차
2. 이 파일
3. [docs/10-calibration-findings.md](docs/10-calibration-findings.md) — 결함 1~17 전체와 처리 상태
4. [docs/11-synthetic-pilot.md](docs/11-synthetic-pilot.md) — 합성 파일럿 S0 결과와 새 발견
5. [docs/99-changelog.md](docs/99-changelog.md) — 설계 결정의 근거

---

## 현재 상태

| 항목 | 상태 |
|---|---|
| 원격 | `github.com/yohan-work/axst` · **공개 저장소** · `main` |
| 커밋 | 25개 push 완료. **이 인계 작업분(아래 "미커밋 변경")은 아직 커밋 안 됨** |
| 린트 | `python3 scripts/lint_items.py` → PASS |
| 문항 | 객관식 24(배포 20 + 예비 4) · 주관식 2 · 전부 `status: 초안` |
| 검증 | C0(채점 리허설) → C1(교차 모델 재검증, 통과) → S0(합성 파일럿 10명, 완결) |
| 온라인 응시본 | https://claude.ai/artifact/1KH1gkFqHsB9H4tWpE4gXt (소유자 비공개 아티팩트) |

### 미커밋 변경 (이 인계 작업)

- `scripts/lint_items.py` — 링크 검사에서 `pilot/responses/`(응답 보관소, gitignore) 제외. 로컬에 복사된 채점 패킷의 상대 링크가 깨져 FAIL 11건이 나던 것을 해결. 역검증 완료(뱅크 안의 깨진 링크는 여전히 FAIL)
- `docs/11-synthetic-pilot.md` — S0 주관식 D1~D4 결과와 새 발견 6건 추가
- `HANDOFF.md`(이 파일) · `AGENTS.md` 포인터 · `docs/99-changelog.md`

커밋은 **사용자가 요청할 때만** 한다. 요청받으면 기능별로 나눠 커밋한다(린터 수정 / S0 기록 / 인계 문서).

---

## 다음 작업 — 우선순위

### P1. `scripts/score.py` 경계 표기 버그 (에이전트가 바로 해도 됨)

**규칙** ([docs/04-scoring.md](docs/04-scoring.md) "총점 → 수준 4단계"): 총점은 ±5점 밴드로 해석하고, 밴드가 경계(39/40 · 59/60 · 79/80)에 걸치면 "L2~L3 경계"로 표기하고 **낮은 쪽 처방**을 적용한다.

**버그**: `level_of()`가 `abs(total - b) <= 0`으로 판정해서 총점이 경계값과 정확히 같을 때만 경계로 표시한다. S0에서 경계여야 할 5명(84·61·56·55·42점)이 하나도 표시되지 않았다.

**기대값** (S0 데이터로 검증):

| 총점 | 기대 표기 |
|---|---|
| 84 | L3~L4 경계 → L3 처방 |
| 73 | L3 |
| 61 · 56 · 55 | L2~L3 경계 → L2 처방 |
| 46 · 45 | L2 |
| 42 | L1~L2 경계 → L1 처방 |
| 23 · 20 | L1 |

**검증 방법**: `python3 scripts/score.py pilot/responses/s0-synthetic/axst-*.json` 출력의 총점 줄이 위 표와 일치하는지 확인. 45점(밴드 40~50)이 경계로 **잡히지 않아야** 한다 — 하한 쪽 오판 확인용.

### P2. 사용자 결정이 필요한 것 — **에이전트가 임의로 반영하지 않는다**

전부 루브릭·규칙 변경이다. 선택지를 정리해 사용자에게 묻고, 결정을 받은 뒤 반영한다.

| # | 내용 | 시급도 | 근거 |
|---|---|---|---|
| 결함 17 | 페이킹 방어 상한의 **발동 조건이 불안정**. 같은 답안에 패스마다 적용 판정이 갈림. 범용 답안이 "자료에 없는 내용은 쓰지 마라" 한 줄로 우회 가능 | 높음 | docs/10. 해법 단서: FR-02 D4 게이트("페르소나 지문에 등장한 실제 단계 이름을 지목")는 위치·대상을 못박아 전 패스가 일관 적용했다 |
| 결함 16 | FIN 페르소나 감점 편향 | 높음 | docs/10. **S0에서 반대 증거 확보**(같은 페르소나 안에서 행동에 따라 감점이 갈림) — 다만 FIN 응답 1건뿐이라 미해소 |
| 결함 14 | D2 요소 ①(수신자·용도) 인정 기준 모호 | 중간 | docs/10 |
| 결함 15 | D4 2점 칸이 수치를 요구하는지 불명 | 중간 | docs/10 |
| **S0-1** | **FR-02 ③ 질문이 D3 5점 요건을 노출** — 21번 D3 5점 0명, 22번 7명 | 높음 | docs/11. 객관식 VER 평균 25%인데 주관식 D3가 높은 어긋남의 유력 원인 |
| **S0-2** | **D2 요소 ⑤를 받은 답안 0/20** — D2 5점 칸 도달 불가 | 중간 | docs/11. 결함 14와 묶어서 결정 |
| **S0-3** | 페이킹 상한이 실제 작성물 80개 점수에서 **한 번도 발동 안 함** | 참고 | docs/11. 결함 17과 묶어서 결정 |

### P3. 문항 수정 후보 — 실제 파일럿에서 재확인 후

| 문항 | 신호 | 비고 |
|---|---|---|
| 11번 `MC-VER-003` | 유일한 음의 변별도(−33%) + 모호 지목 4명 | 합성 데이터라 `p`·`d`는 참고만. "가장 먼저 확인할 것" 유형에서 우선순위 규칙 적용이 갈림 |
| 18번 `MC-FLW-004` | 모호 지목 5명(상·하위 무관) | 최선–최악의 최악이 선택지에서 갈리지 않음 |

문항을 고치면: [docs/01-axes.md](docs/01-axes.md) 결정트리 → [docs/03-item-writing-rules.md](docs/03-item-writing-rules.md) → 작성 → `python3 scripts/build_web_form.py` → `python3 scripts/lint_items.py` → `docs/99-changelog.md`에 한 줄. `forms/form-A.md`·`form-A.answers.md`·`MANIFEST.yml`은 손으로 갱신하고 린트가 파생값을 대조한다.

### P4. 사람이 해야 하는 것 — 에이전트가 대신할 수 없다

- **소요 시간 실측** — 온라인 응시본을 사람이 실제로 풀면 구간별 시간이 자동 기록된다. 추정치는 32~35분
- **스폰서 합의문 서명** — [docs/09-sponsor-agreement.md](docs/09-sponsor-agreement.md). 서명 없이 시행하지 않는다
- **실제 파일럿 10~15명** — 레벨을 섞어 선정. S0는 이것을 대체하지 못한다(정답률·변별도·시간은 사람에게서 다시 잰다)

---

## 작업 규율 — 이 레포에서 세 번 데었다

1. **검사를 추가하면 그 자리에서 고의로 깨뜨려 FAIL을 확인하고 원복한다.** 정답지·MANIFEST의 정답을 조작해도 린트가 PASS하던 구멍이 있었고, 파생값 검사 작성 중 정규식 버그로 단독 문항 8개를 놓친 적이 있다.
2. **파싱할 섹션을 못 찾으면 폴백하지 말고 실패시킨다.** 측정 스크립트가 섹션을 못 찾고 파일 전체로 폴백해 글자수가 2배로 부풀었고, 존재하지 않는 결함 2건을 보고했다가 철회했다.
3. **에이전트(서브에이전트 포함)의 자기 보고를 근거로 쓰지 않는다. 원문을 읽는 패스만이 근거다.** 합성 응시자가 "위반을 심었다"고 보고했으나 실제 답안에는 없었고 감점 패스가 반증했다.
4. **LLM 채점은 차원별 독립 컨텍스트로 돌린다.** 한 번에 4차원을 채점하지 않는다. 캘리브레이션 회차에는 판단형 패스에 앵커를 주지 않는다(순환). [rubrics/llm-scorer-prompt.md](rubrics/llm-scorer-prompt.md).
5. **앵커를 추가할 때 답안을 쓴 쪽이 점수까지 매기지 않는다.** 점수를 비운 채 확정 앵커를 대조군으로 섞어 다른 모델에 먼저 채점시키고 수렴할 때만 확정한다.

## 불가침 규칙 (요약 — 정본은 AGENTS.md)

- 축별 점수를 숫자로 내보내지 않는다. 3단 플래그만
- 개인 리포트에 축별 레이더 차트를 그리지 않는다
- 툴 상식 문항을 추가하지 않는다. 허용 유형은 SJT · 산출물비평 · 최선차선
- 교육 설계용과 인사 평가용을 한 폼으로 겸하지 않는다
- `pilot/responses/`를 커밋하지 않는다

## 공개 저장소 제약

저장소가 공개라 **정답지와 문항의 정답·해설이 공개돼 있다.** 사용자가 알고 공개를 유지했다. 따라서 **폼 A로는 점수가 나가는 진단 회차를 시행하지 않는다** — 시행은 비공개 시행 폼으로 한다. 린트가 강제한다(`MANIFEST.yml`의 `visibility: 공개참조` 폼은 `exposures`가 0을 넘으면 FAIL). 파일럿은 폼 A로 해도 된다. 상세: [docs/08-administration.md](docs/08-administration.md) "폼 분리". 라이선스는 미지정(법적 기본값은 전권 유보).

---

## 로컬 전용 자료 (git에 없음)

`pilot/responses/s0-synthetic/` — `.gitignore` 대상이라 **이 PC에만 있다.** 새로 클론한 환경에는 없다.

| 파일 | 내용 |
|---|---|
| `axst-*.json` (10개) | 합성 응시자 응답. `fr_scores`까지 기입 완료 |
| `fr/pass-{D1,D2,D3,D4,penalty}.md` | 주관식 채점 패킷 |
| `exam.md` | 정답 없는 응시 패킷 |
| `fr-scores-partial.md` | 차원별 점수표 (이제 D1까지 전부 완료 — 파일명만 partial) |
| `README-재개.md` | 재개 절차 (작성 시점 기준, 현재는 완료 상태) |

## 명령어

```bash
python3 scripts/lint_items.py                                   # 커밋 전 필수
python3 scripts/build_web_form.py                               # 문항 수정 시 온라인 응시본 재생성
python3 scripts/score.py pilot/responses/s0-synthetic/axst-*.json            # 개인 채점표
python3 scripts/score.py pilot/responses/s0-synthetic/axst-*.json --cohort   # 조직 집계 (n≥10)
python3 scripts/score.py <응답들>.json --prompts out/            # 주관식 5패스 패킷
```

의존성 없음(Python 표준 라이브러리만).
